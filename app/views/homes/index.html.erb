
      <div class="jumbotron title">
        <nav class="navbar navbar-default" style="margin-bottom:0; background-color:transparent;">

            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <div class="navbar-brand">
                    <i class="fa fa-child" aria-hidden="true"></i>
                    <%= link_to "Scout" , root_path %>
                </div>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

                <% if signed_in? %>
                <ul class="nav navbar-nav navbar-right">
                  <li><%= link_to "Add Place", new_place_path, method: :get %></li>
                  <li><%= link_to "Cities", places_path, method: :get %></li>
                  <li><%= link_to "My Runs", user_path(current_user) %></li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email %>
                            <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><%= link_to "My Profile", user_path(current_user) %></li>
                            <li><%= link_to "Log Out", sign_out_path, method: :delete %></li>

                        </ul>

                </ul>
            </li>
        <% else %>
            <ul class="nav navbar-nav navbar-right">
                <li><%= link_to "Log in", sign_in_path %></li>
                <li><%= link_to "Sign up", sign_up_path %></li>
            </ul>
            <% end %>

    </ul>
</div>
</nav>
<!-- /.navbar-collapse -->

          <div class="container-fluid">
            <div class="container">


              <h1 class="title text-center" style="margin-left: auto; margin-right: auto; width: 1000px; height: 80px; font-family: 'Optima';">Where do you want to run?</h1>

              <%= form_tag places_path, :method => 'get', style: "width: 40%; margin-left: auto; margin-right: auto;" do %>
                    <div class="input-group">
                        <%= text_field_tag :query, params[:query], :class => 'form-control', :placeholder => 'Search for a city...' %>
                        <span class="input-group-btn">
                            <%= submit_tag "Get moving", :name => nil, :class => 'btn btn-default'%>
                        </span>
                        <% end %>
                    </div>
</div>
</div>
    </div>

<div class ="container">
  <div class="row">
        <div class="col-sm-1">
        </div>
        <div class="col-sm-10">
        <h1 class="text-center font">
        <% @cities.each do |city| %>
          <%= link_to places_path(query: city.name)do%>

          <p class="padding"><%=image_tag city.photo[0].thumb.url if city.photo.any?%></p>
        <% end %>
        <% end %>


        </div>
      <div class="col-sm-1">
    </div>
  </div>
